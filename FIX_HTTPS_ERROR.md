# 🔒 رفع خطای HTTPS در سرور Development

## 🔍 مشکل

خطای شما:
```
You're accessing the development server over HTTPS, but it only supports HTTP.
```

این خطا یعنی شما در حال دسترسی به سرور Django از طریق **HTTPS** هستید، اما سرور development Django فقط **HTTP** را پشتیبانی می‌کند.

## ✅ راه حل ساده

### از آدرس HTTP استفاده کنید، نه HTTPS

**❌ اشتباه:**
```
https://127.0.0.1:8000
https://localhost:8000
```

**✅ درست:**
```
http://127.0.0.1:8000
http://localhost:8000
```

## 📝 راه حل‌های مختلف

### راه حل 1: تغییر آدرس در مرورگر

1. نوار آدرس مرورگر را پیدا کنید
2. `https://` را به `http://` تغییر دهید
3. Enter بزنید

**مثال:**
- قبل: `https://127.0.0.1:8000/login/`
- بعد: `http://127.0.0.1:8000/login/`

### راه حل 2: استفاده از localhost

به جای IP، از `localhost` استفاده کنید:

```
http://localhost:8000
```

### راه حل 3: پاک کردن Cache مرورگر

گاهی مرورگر آدرس قبلی را به خاطر دارد:

1. **Chrome/Edge:**
   - `Ctrl + Shift + Delete`
   - تیک "Cached images and files" را بزنید
   - Clear data

2. **Firefox:**
   - `Ctrl + Shift + Delete`
   - تیک "Cache" را بزنید
   - Clear Now

3. یا در حالت Incognito/Private باز کنید:
   - Chrome: `Ctrl + Shift + N`
   - Firefox: `Ctrl + Shift + P`

### راه حل 4: استفاده از Port متفاوت

اگر مشکلی با port 8000 دارید:

```powershell
python manage.py runserver 8080
```

سپس به آدرس `http://127.0.0.1:8080` بروید.

## 🔧 اگر نیاز به HTTPS دارید (برای Development)

اگر واقعاً نیاز به HTTPS دارید (برای تست SSL)، می‌توانید از `django-extensions` استفاده کنید:

### نصب:

```powershell
pip install django-extensions Werkzeug
```

### اجرا با HTTPS:

```powershell
python manage.py runserver_plus --cert-file cert.pem
```

اما این فقط برای development است و **هرگز برای production استفاده نشود**.

## ⚠️ نکات مهم

1. **سرور Development فقط برای تست است:**
   - فقط HTTP پشتیبانی می‌کند
   - نباید در production استفاده شود
   - فقط برای توسعه و تست مناسب است

2. **برای Production:**
   - از Nginx + Gunicorn استفاده کنید
   - یا از Apache + mod_wsgi
   - یا از سرویس‌های cloud مثل Heroku, AWS, etc.
   - این سرویس‌ها HTTPS را پشتیبانی می‌کنند

3. **مرورگر ممکن است به HTTPS redirect کند:**
   - اگر قبلاً از HTTPS استفاده کرده‌اید
   - Cache مرورگر را پاک کنید
   - یا از حالت Incognito استفاده کنید

## 📋 دستورات سریع

```powershell
# 1. اطمینان از فعال بودن virtual environment
D:\.school_env\Scripts\Activate.ps1

# 2. اجرای سرور
python manage.py runserver

# 3. در مرورگر به آدرس زیر بروید:
# http://127.0.0.1:8000
```

## ✅ بررسی موفقیت

بعد از تغییر به HTTP، باید:
- ✅ سرور بدون خطا اجرا شود
- ✅ صفحه لاگین نمایش داده شود
- ✅ خطای HTTPS دیگر نمایش داده نشود

## 🆘 اگر مشکل ادامه داشت

1. **بررسی آدرس URL:**
   - مطمئن شوید که `http://` استفاده می‌کنید (نه `https://`)
   - مطمئن شوید که Port درست است (معمولاً 8000)

2. **بررسی سرور:**
   - مطمئن شوید که سرور در حال اجرا است
   - به پیام‌های کنسول نگاه کنید

3. **بررسی Firewall:**
   - مطمئن شوید که Firewall مانع نمی‌شود

4. **ریستارت سرور:**
   ```powershell
   # متوقف کردن سرور (Ctrl + C)
   # سپس دوباره اجرا کنید:
   python manage.py runserver
   ```

---

**خلاصه:** فقط کافیست در مرورگر `https://` را به `http://` تغییر دهید! 🎯

